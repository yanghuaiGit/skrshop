# application.yml
spring:
  application:
    name: mall
  profiles:
    active: dev
server:
  port: 8082
  servlet:
    context-path: /skrshop/mall
mybatis:
  mapper-locations:  classpath*:mapper/**/*.xml
